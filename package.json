{
  "name": "novu_builder",
  "version": "1.0.0",
  "description": "Foundation repo for the FotoNabidka / NOVU Builder product.",
  "main": "server/app.js",
  "scripts": {
    "test": "node tests/api.smoke.test.js",
    "start": "node server/app.js",
    "api:start": "node server/app.js",
    "web:dev": "npm --prefix novu-react run dev",
    "web:build": "npm --prefix novu-react run build",
    "web:lint": "npm --prefix novu-react run lint",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:bootstrap": "node scripts/bootstrap_sqlite_db.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
